# Structure
```text
GameIdeas/
├── .github/
│   └── workflows/
│       └── build.yml # creating for .exe, os-app and ux.file thingy
│
├── THE_GAME/ # game that simulates the card game THE GAME
```

# install 
 
Start the creation of the executable files for Win, Lin & Os:

```v*``` is every version I want to get an executable file for e.g.: ```v6.4``` 

```text
git tag v*
git push origin v*
```